name: Build Android (Briefcase)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      # Installs Android SDK tools + accepts licenses + puts sdkmanager/adb on PATH
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Briefcase
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -U briefcase
          briefcase --version

      - name: Create/Build/Package (Android)
        run: |
          briefcase create android -v
          briefcase build android -v
          briefcase package android -v

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-dist
          path: dist/**
